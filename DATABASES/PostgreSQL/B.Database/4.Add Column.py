#to Add Column to an existing table we use the command "ALTER TABLE" statement
#the ALTER TABLE statement is used to add,delete,or modify columns in a table

# Add Column
#We add column color to the bikes table

# ALTER TABLE bikes
# ADD color VARCHAR(255);
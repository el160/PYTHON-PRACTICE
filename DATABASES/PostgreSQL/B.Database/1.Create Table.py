#create a new database using SQL shell once you are connected to the data ,write SQL statements

# Create Table
#the following command will create a table in the  postgress data
# CREATE TABLE bikes (
#   brand VARCHAR(255),
#   model VARCHAR(255),
#   year INT
# );

# the above statement  creates an empty table 
# you can display the empty table using the following command
# SELECT * FROM bikes;
#the above statement will give a result below
# brand | model | year
# -------+-------+------
# (0 rows

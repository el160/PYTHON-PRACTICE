# we use the "update" statement to update the table
# UPDATE bikes
# SET color = 'red'
# WHERE brand = 'Bmw';

# note that without the WHERE clause all records wil be updated

# Updating Multiiple columns
# to update more than one column we seperate the name/value pairs using comma
# UPDATE bikes
# SET color = 'white', year = 1970
# WHERE brand = 'Suzuki';
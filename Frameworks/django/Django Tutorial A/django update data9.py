# to update records in the databse we have to first get the record we want to update
# we use the following command to get to the record:
#'from players.models import Player
#y = Player.objects.all ()[3]' y rep player at index 3

# now to change values in the record
 # y.age = 32
 # y.save()  # this will save the changes to the database

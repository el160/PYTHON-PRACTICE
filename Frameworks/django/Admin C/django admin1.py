# main interface that does all the CRUD opersations
# its free and comes ready to use with django
# to enter admin interface start the sever by navigating to env folder and run command python manage.py runserver
# in urls.py of the project there is a path that connects admin route and sends them to admin.site.ursls
# from django.contrib import admin
# from django.urls import include, path

# urlpatterns = [
#     path('', include('players.urls')),
#     path('admin/', admin.site.urls),
# ]

# django admin has alot of built in functionality that can be used to manage the data in the database one of them being the login user interfaces

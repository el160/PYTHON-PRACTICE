#postgresql is a relational opensource database system that works well with django
#it is used for deployment purposes since the dbsqlite3 that is inbuilt with django is best for development purposes
#postgresql is reliable and works well with various django projects
# to be to use postgresql we nedd to install pyscopg2 package that is necessarry for the database to work
#the pyscopg2 package driver is installed in the virtual environment using the following command
# "pip install psycopg2-binary"
